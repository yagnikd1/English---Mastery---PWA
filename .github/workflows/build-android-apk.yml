name: Build English Mastery APK

on:
  push:
    paths:
      - 'android-wrapper/**'
      - '.github/workflows/build-android-apk.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-wrapper

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Build debug APK
        run: gradle --no-daemon assembleDebug

      - name: Prepare named APK
        run: cp app/build/outputs/apk/debug/app-debug.apk app/build/outputs/apk/debug/English_Mastery_Android.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: English-Mastery-Android-App
          path: android-wrapper/app/build/outputs/apk/debug/English_Mastery_Android.apk
          if-no-files-found: error
          retention-days: 30
